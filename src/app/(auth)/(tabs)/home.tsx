import DatePicker from "@/src/components/DatePicker";
import { Text, View } from "react-native";

const Home = () => {
  return (
    <View
      style={{
        flex: 1,
      }}
    >
      <DatePicker />
    </View>
  );
};

export default Home;
