import { Text, View } from "react-native";

const Header = () => {
  return (
    <View>
      <Text className="text-center text-2xl font-bold color-red-700">
        Header
      </Text>
    </View>
  );
};

export default Header;
